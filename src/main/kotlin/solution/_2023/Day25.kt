package solution._2023

import com.github.shiguruikai.combinatoricskt.combinations
import kotlin.math.abs
import kotlin.math.ceil
import kotlin.math.floor
import kotlin.math.sqrt
import kotlinx.serialization.*
import kotlinx.serialization.json.*
import util.*
import util.ints.product
import util.longs.product
import util.mapping.Coord
import util.mapping.Direction

class Day25(val input: List<String>) {
    fun solve1(): Long {

//        val nodesNames = input.flatMap { it.words().toSet() }.toSet()
//        val connections = input.flatMap { str ->
//            val words = str.words()
//
//            words.drop(1).map { setOf(it, words.first()) }
//        }
//
//
//        val graph = nodesNames.map { name ->
//            connections.filter { name in it }
//        }



        return 0L
    }

}